# September 18, 2026 release

This release brings together the work since production commit `a84cfc7`: mobile interview layouts, refined case visuals, Rhea's persona and evaluation harnesses, LangGraph orchestration, and timed interviews. Historical assessment and model-run reports remain dated records, not current release guarantees.

## Changes

| Area | Result |
|---|---|
| Player phones | Now / Case file / Team navigation, compact evidence picker, collapsed earlier transcript and less repeated artwork. |
| Visuals | YouTube channel exhibit with thumbnails; Grey Lady faces the viewpoint at an unidentifiable distance; focused newly released evidence. |
| Interview engine | LangGraph evaluates eligible discoveries, prepares approved facts, drafts, validates, repairs once if needed and falls back safely. Durable state stays in Supabase. |
| Rhea | Guarded, defensive delivery; appropriate alibi after exposure; concealment motive approved only after both financial and CCTV admissions. |
| Evals | All-suspect evidence contracts/cues/conversations, independent calibrated grading and focused Rhea production-graph scenarios. |
| Timing | 480 active seconds per suspect, 90 seconds per detective, early pass and 120-second host extension. AI generation pauses both clocks; revisits preserve budgets. |
| Documentation | README, architecture/diagrams, PRD status, handoff, authoring, database and case guides reconciled; historical assessments linked to current status. |

## Verification

Local baseline: 164 unit tests passed, one environment-dependent test skipped; 197 API integration assertions passed; transactional SQL timing checks passed; lint and production build passed. Host countdown and extension control were inspected in the browser. Refreshed lint, 164 passing unit tests, case validation, all 30 printable checks, and dependency audit passed (zero findings). Seven documentation diagrams were regenerated. Production evidence is recorded below.

Focused Rhea tuning produced 12 delivered answers with expected unlocks and no execution errors; manual review retains the caveat that an admission can omit a mandatory detail. The full earlier all-suspect run had unresolved quality findings. Rhea phone records and Devraj duty log remain missing authored exhibits; the offline investigator audit intentionally reports those gaps. These are documented limitations of this prototype, not newly verified features.

## Rollout

Apply the additive timed-interviews migration before pushing the application to production. It preserves existing sessions, transcripts and discoveries, initializes budgets on use, and restricts clock mutation RPCs to the service role. Production is the Git-connected Vercel project `party-game` and Supabase project `nfpochmyqmttqirhflac`.

Supabase migration `20260918234906_timed_interviews.sql` was applied on September 18 via the connected integration. Hosted migration history matches the checked-in filename. Both clock RPCs are security-invoker and executable only by the service role; no new security warning/error was reported. Three existing informational notices concern intentionally private RLS tables without client policies ([advisor explanation](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy)). Application deployment verification is pending. No physical eight-device or load rehearsal is claimed. Solo gameplay, a hacking minigame and inline final-video playback remain deferred.
